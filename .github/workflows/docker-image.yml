name: 'Docker 镜像构建'
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'          # 打 v0.1.0 等标签时自动构建并产出镜像
  release:
    types: [published, released]
permissions:
  contents: write

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: 'Go 鍗曞厓娴嬭瘯'
      uses: actions/setup-go@v7
      with:
        go-version: '1.25'
    - name: '杩愯娴嬭瘯'
      run: go test ./...
    - name: '构建 Docker 镜像'
      run: |
        docker build -t simplewebshell-remoteupdate .
    - name: '导出并上传镜像工件'
      run: |
        docker save simplewebshell-remoteupdate | gzip > simplewebshell-remoteupdate-image.tar.gz
    - uses: actions/upload-artifact@v4
      with:
        name: 'simplewebshell-remoteupdate-docker-image-${{ github.ref_name }}'
        path: 'simplewebshell-remoteupdate-image.tar.gz'
        if-no-files-found: 'warn'
        retention-days: 14
    - name: '上传镜像包到 Release'
      if: github.event_name == 'release' || startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.event.release.tag_name || github.ref_name }}
        files: 'simplewebshell-remoteupdate-image.tar.gz'
        fail_on_unmatched_files: false
